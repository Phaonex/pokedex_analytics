import { PokedexValidationPipe } from './pokedex.validation.pipe';

describe('PokedexValidationPipe', () => {
  it('should be defined', () => {
    expect(new PokedexValidationPipe()).toBeDefined();
  });
});
